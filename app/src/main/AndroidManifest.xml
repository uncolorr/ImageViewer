<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.colorblind.uncolor.imageviewer">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".application.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".images_activity.ImagesActivity_"
            android:label="Images"
            android:screenOrientation="portrait"
            android:theme="@style/WhiteAppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".image_slider_activity.SwipeImagesActivity_"
            android:screenOrientation="portrait"
            android:theme="@style/ImageViewAppTheme"/>
    </application>

</manifest>